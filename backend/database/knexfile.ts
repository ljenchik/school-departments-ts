const config:{[name: string]: any} = {
  development: {
    client: "pg",
    connection: {
      database: "School-ts",
      user:     "Olena",
      password: "1234"
    }
  }
}

export default config;
